{% extends 'base.html.twig' %}

{% block title %}Hello World !{% endblock %}

{% block body %}
<p>page acceuil</p>
    <p>Nous sommes le {{ 'now'|date('D d/m/Y H:i') }}</p>
    <!--Petit exemple de fonctionnalitées-->
    <p>{{ bidule|default ('<p>je suis une utilisation de default et raw</p>')|raw }}</p>

    <a href="{{ path('app_wish') }}">vers liste</a>
    <a href="{{ path('app_about') }}">vers about</a>
{% endblock %}
